# ================================================================
# Notification Service — DOCKER Profile
# ================================================================
spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/notif_db
    username: ${DB_USER:guruai}
    password: ${DB_PASSWORD}

  kafka:
    bootstrap-servers: kafka:9092
