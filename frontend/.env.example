# Copy to .env.local for local dev (Vite auto-loads .env.local, gitignored).
# Everything goes through the API gateway — the frontend never calls a
# downstream service (auth, document, quiz, ...) directly.
VITE_API_BASE_URL=http://localhost:8080
VITE_WS_BASE_URL=ws://localhost:8080
