# ================================================================
# Document Service — LOCAL Profile (IDE / IntelliJ Run)
# ================================================================
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/document_db
    username: guruai
    password: guruai_secret_2026
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2

  data:
    redis:
      host: localhost
      password: guruai_redis_2026

  kafka:
    bootstrap-servers: localhost:9094

  # For local IDE runs, set GOOGLE_API_KEY in your IDE run configuration
  # or in a local .env file loaded by your IDE plugin. (This service is
  # Gemini-only — Groq is used by the conversational services instead.)

logging:
  level:
    com.guruai.document: DEBUG
    org.springframework.ai: DEBUG
